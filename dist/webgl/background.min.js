(function(){"use strict";const x=window.__yaoxiRTG&&window.__yaoxiRTG.config||{},E=x.shaderPath||null,F=x.cardSelector||".news-card, .slide-text",A=8,b=x.marchSteps||64,N=`
#version 300 es
precision highp float;
layout(location = 0) in vec2 aPos;
out vec2 vUv;
void main() {
    vec2 p = aPos;
    vUv = p * 0.5 + 0.5;
    gl_Position = vec4(p, 0.0, 1.0);
}
`,_=`
#version 300 es
precision highp float;
uniform vec2  uResolution;
uniform vec2  uSunPos;
uniform vec3  uSunDir;
uniform vec3  uSunColor;
uniform vec3  uSkyZenith;
uniform vec3  uSkyHorizon;
uniform vec3  uAmbient;
uniform vec3  uGround;
uniform float uSunElev;
uniform float uTime;
uniform float uScrollY;
uniform float uDPR;
uniform float uWeather;
uniform float uRain;
uniform float uTheme;
uniform int   uSteps;
uniform int   uCardCount;
uniform vec4  uCards[8];
uniform vec2  uCursor;
uniform float uCursorL;
in vec2 vUv;
out vec4 fragColor;
float hash21(vec2 p) {
    p = fract(p * vec2(123.34, 456.21));
    p += dot(p, p + 45.32);
    return fract(p.x * p.y);
}
float vnoise(vec2 p) {
    vec2 i = floor(p);
    vec2 f = fract(p);
    vec2 u = f * f * (3.0 - 2.0 * f);
    float a = hash21(i);
    float b = hash21(i + vec2(1.0, 0.0));
    float c = hash21(i + vec2(0.0, 1.0));
    float d = hash21(i + vec2(1.0, 1.0));
    return mix(mix(a, b, u.x), mix(c, d, u.x), u.y);
}
float fbm(vec2 p) {
    float s = 0.0;
    float a = 0.5;
    for (int i = 0; i < 3; i++) {
        s += a * vnoise(p);
        p = p * 2.03 + 17.1;
        a *= 0.5;
    }
    return s;
}
float fbm2(vec2 p) {
    float s = 0.0;
    float a = 0.6;
    for (int i = 0; i < 2; i++) {
        s += a * vnoise(p);
        p = p * 2.13 + 11.7;
        a *= 0.5;
    }
    return s;
}
vec3 skyBase(vec2 px) {
    vec2 uv = px / uResolution;
    float he = clamp(uv.y, 0.0, 1.0);
    vec3 sky = mix(uSkyHorizon, uSkyZenith, pow(he, 0.45));
    float sunUp = smoothstep(-0.15, 0.22, uSunElev);
    vec2 toSun = uSunPos - px;
    float sd = length(toSun);
    float disc = exp(-sd * sd * 0.9);
    float glow = exp(-sd * sd * 0.0006);
    float haze = exp(-sd * sd * 0.00018);
    sky += uSunColor * (disc * 1.5 + glow * 0.12 + haze * 0.30) * sunUp;
    float g = smoothstep(0.55, 0.62, uv.y);
    sky = mix(sky, uGround, g * 0.88);
    float stars = pow(max(vnoise(px * 0.02 * uDPR) - 0.966, 0.0) * 30.0, 2.0);
    stars *= (1.0 - sunUp) * smoothstep(0.30, 0.55, uv.y);
    sky += vec3(0.8, 0.85, 1.0) * stars * 0.5;
    return sky;
}
vec3 godRays(vec2 px) {
    float sunUp = smoothstep(-0.05, 0.25, uSunElev);
    float clearK = (1.0 - uWeather);
    vec3 beam = vec3(0.0);
    vec2 dir = (uSunPos - px) / max(float(uSteps), 1.0);
    float t = 0.0;
    float scale = uResolution.y * 0.0016;
    for (int i = 0; i < uSteps; i++) {
        vec2 sp = px + dir * t;
        float dens = fbm2(sp * scale + vec2(0.0, uTime * 0.02));
        float occ = smoothstep(0.42, 0.64, dens);
        beam += uSunColor * (1.0 - occ) * (1.0 - t * 0.012) * 0.05;
        t += 1.0;
    }
    return beam * sunUp * clearK * 0.55;
}
vec3 dust(vec2 px, float sunUp, float clearK) {
    vec3 c = vec3(0.0);
    vec2 q = px * 0.0022 * uDPR + vec2(uTime * 0.030, uTime * 0.015);
    float n = fbm(q);
    float spark = pow(max(n - 0.52, 0.0) * 6.0, 2.2);
    float dSun = length(uSunPos - px);
    float sunNear = exp(-dSun * 0.0045);
    c += uSunColor * spark * sunNear * 0.45 * clearK * sunUp;
    c += uAmbient * spark * 0.10 * clearK;
    return c;
}
float rainStreaks(vec2 p, float t) {
    float s = 0.0;
    for (int i = 0; i < 4; i++) {
        vec2 g = vec2(float(i) * 7.13, float(i) * 13.7);
        vec2 cell = floor(p * 16.0 + g);
        float h = hash21(cell);
        vec2 o = vec2(h, fract(h * 7.31));
        vec2 q = fract(p * 16.0 + g) - o;
        vec2 d = normalize(vec2(0.35, -1.0));
        float along = dot(q, d) + t * (7.0 + float(i) * 2.0) + o.x * 5.0;
        float across = abs(dot(q, vec2(d.y, -d.x)));
        float lenM = smoothstep(0.0, 0.02, along) * (1.0 - smoothstep(0.02, 0.22, along));
        float widM = 1.0 - smoothstep(0.0, 0.018, across);
        s += lenM * widM * step(0.45, h);
    }
    return s * 0.5;
}
vec2 ripples(vec2 p, float t) {
    vec2 acc = vec2(0.0);
    for (int i = 0; i < 6; i++) {
        vec2 g = vec2(float(i) * 21.31, float(i) * 37.71);
        vec2 cell = floor(p * 5.0 + g);
        float ha = hash21(cell);
        float hb = hash21(cell + 7.31);
        vec2 cc = cell + vec2(ha, hb);
        vec2 d = p * 5.0 + g - cc;
        float dist = length(d);
        float speed = 3.2 + float(i) * 0.9;
        float phase = dist * 24.0 - t * speed;
        float amp = exp(-dist * 4.2) * (0.5 + 0.5 * sin(phase * 0.4));
        float w = 1.0 - smoothstep(0.25, 1.0, dist);
        acc += vec2(cos(phase), sin(phase)) * amp * w * (0.4 + 0.6 * ha);
    }
    return acc * 0.14;
}
float rectMask(vec2 p, vec4 r) {
    vec2 d0 = p - r.xy;
    vec2 d1 = r.xy + r.zw - p;
    float mx = min(min(d0.x, d0.y), min(d1.x, d1.y));
    return smoothstep(0.0, 2.0, mx);
}
float edgeDist(vec2 p, vec4 r) {
    vec2 h = r.zw * 0.5;
    vec2 c = r.xy + h;
    vec2 q = abs(p - c) - h;
    float d = length(max(q, 0.0)) + min(max(q.x, q.y), 0.0);
    return -d;
}
float ggxD(float NoH, float a2) {
    float d = NoH * NoH * (a2 - 1.0) + 1.0;
    return a2 / max(3.14159265 * d * d, 1e-5);
}
float schlickF(float NoV, float f0) {
    return f0 + (1.0 - f0) * pow(1.0 - NoV, 5.0);
}
vec3 cardSplashes(vec2 px, vec4 r, float t, float rain) {
    vec3 s = vec3(0.0);
    for (int i = 0; i < 6; i++) {
        float hx = hash21(vec2(float(i) * 3.7, 1.3));
        float hy = hash21(vec2(float(i) * 9.1, 5.7));
        vec2 sp = vec2(r.x + hx * r.z, r.y + r.w - hy * 6.0);
        float d = length(px - sp);
        float pulse = 0.5 + 0.5 * sin(t * 26.0 + float(i) * 2.6);
        float cycle = fract(hx * 7.0 + t * 0.5);
        float life = smoothstep(0.0, 1.0, cycle) * (1.0 - smoothstep(0.0, 1.0, cycle));
        float m = exp(-d * d * 0.06) * pulse * life;
        s += vec3(0.75, 0.9, 1.0) * m;
    }
    return s * rain * 1.1;
}
void main() {
    vec2 px = gl_FragCoord.xy;
    float sunUp = smoothstep(-0.05, 0.25, uSunElev);
    float clearK = (1.0 - uWeather);
    vec2 basePx = px + vec2(0.0, uScrollY * 0.16);
    vec2 dp = basePx / uResolution.y;
    vec2 rip = ripples(dp, uTime);
    vec2 refr = rip * (14.0 * uRain);
    vec3 beam = godRays(basePx);
    vec3 bg = skyBase(basePx) + beam + dust(basePx, sunUp, clearK);
    vec3 atmoRefr = skyBase(px + refr) + beam + dust(px + refr, sunUp, clearK);
    float streak = rainStreaks(dp, uTime);
    vec3 rainBg = atmoRefr * (0.86 + 0.20 * streak) + uAmbient * 0.10;
    bg = mix(bg, rainBg, uWeather);
    vec3 tint = mix(vec3(0.55, 0.65, 0.82), vec3(0.93, 0.96, 1.0), uTheme);
    float glassA = mix(0.88, 0.72, uTheme);
    vec3 V = vec3(0.0, 0.0, 1.0);
    vec3 N = normalize(vec3(rip * 0.5, 1.0));
    float NoV = max(dot(N, V), 0.0);
    float roug = 0.34;
    float a2 = roug * roug * roug * roug;
    float dispK = mix(0.5, 1.8, uRain) * (0.3 + 0.7 * sunUp);
    vec3 Hs = normalize(V + uSunDir);
    float NoH = max(dot(N, Hs), 0.0);
    float NoL = max(dot(N, uSunDir), 0.0);
    float Ds = ggxD(NoH, a2);
    float Fs = schlickF(NoV, 0.04);
    vec3 sunSpec = uSunColor * Ds * Fs * NoL * sunUp * 0.28;
    vec3 Lc = normalize(vec3(uCursor - px, -uResolution.y * 0.55));
    vec3 Hc = normalize(V + Lc);
    float NoHc = max(dot(N, Hc), 0.0);
    float NoLc = max(dot(N, Lc), 0.0);
    float Dc = ggxD(NoHc, a2);
    float Fc = schlickF(NoV, 0.04);
    vec3 curSpec = vec3(1.0, 0.98, 0.94) * Dc * Fc * NoLc * uCursorL * 0.6;
    float inter = 0.5 + 0.5 * sin(NoH * 40.0 + (1.0 - roug) * 14.0);
    vec3 irid = mix(vec3(0.25, 0.6, 1.0), vec3(1.0, 0.35, 0.55), inter);
    vec3 spec = (sunSpec + curSpec) * (0.55 + 0.45 * irid);
    vec3 chroma = vec3(0.0);
    chroma.r = skyBase(px + refr + vec2(3.0, 0.0)).r;
    chroma.g = skyBase(px + refr + vec2(0.0, 0.0)).g;
    chroma.b = skyBase(px + refr + vec2(-3.0, 0.0)).b;
    vec3 dispersion = (chroma - atmoRefr) * dispK;
    vec3 glassAcc = vec3(0.0);
    float cov = 0.0;
    for (int i = 0; i < uCardCount; i++) {
        vec4 r = uCards[i];
        float m = rectMask(px, r);
        float edge = edgeDist(px, r);
        float inEdge = (1.0 - exp(-edge * 0.06)) * step(0.0, edge);
        vec3 col = atmoRefr * tint
                 + dispersion * inEdge
                 + spec
                 + uSunColor * Fs * inEdge * sunUp * 0.9
                 + cardSplashes(px, r, uTime, uRain);
        glassAcc += col * m;
        cov += m;
    }
    cov = clamp(cov, 0.0, 1.0);
    vec3 final = mix(bg, glassAcc, cov * glassA);
    vec2 qv = px / uResolution - 0.5;
    final *= 1.0 - dot(qv, qv) * 0.35;
    final += (hash21(px) - 0.5) * (1.5 / 255.0);
    fragColor = vec4(final, 1.0);
}
`,H=`
#version 300 es
precision highp float;
uniform vec2  uResolution;
uniform vec2  uSunPos;
uniform vec3  uSunColor;
uniform vec3  uSkyZenith;
uniform vec3  uSkyHorizon;
uniform vec3  uGround;
uniform float uSunElev;
uniform float uScrollY;
uniform float uDPR;
uniform vec2  uCursor;
uniform float uCursorL;
in vec2 vUv;
out vec4 fragColor;
float hash21(vec2 p) {
    p = fract(p * vec2(123.34, 456.21));
    p += dot(p, p + 45.32);
    return fract(p.x * p.y);
}
float vnoise(vec2 p) {
    vec2 i = floor(p);
    vec2 f = fract(p);
    vec2 u = f * f * (3.0 - 2.0 * f);
    float a = hash21(i);
    float b = hash21(i + vec2(1.0, 0.0));
    float c = hash21(i + vec2(0.0, 1.0));
    float d = hash21(i + vec2(1.0, 1.0));
    return mix(mix(a, b, u.x), mix(c, d, u.x), u.y);
}
void main() {
    vec2 px = gl_FragCoord.xy + vec2(0.0, uScrollY * 0.16);
    vec2 uv = px / uResolution;
    vec3 sky = mix(uSkyHorizon, uSkyZenith, pow(clamp(uv.y, 0.0, 1.0), 0.45));
    float sunUp = smoothstep(-0.15, 0.22, uSunElev);
    float sd = length(uSunPos - px);
    sky += uSunColor * (exp(-sd * sd * 0.9) * 1.5 + exp(-sd * sd * 0.0006) * 0.12 + exp(-sd * sd * 0.00018) * 0.30) * sunUp;
    float g = smoothstep(0.55, 0.62, uv.y);
    sky = mix(sky, uGround, g * 0.88);
    float stars = pow(max(vnoise(px * 0.02 * uDPR) - 0.966, 0.0) * 30.0, 2.0);
    stars *= (1.0 - sunUp) * smoothstep(0.30, 0.55, uv.y);
    sky += vec3(0.8, 0.85, 1.0) * stars * 0.5;
    float cd = length(uCursor - gl_FragCoord.xy);
    sky += vec3(1.0, 0.98, 0.94) * exp(-cd * cd * 0.00002) * uCursorL * 0.30;
    vec2 qv = uv - 0.5;
    sky *= 1.0 - dot(qv, qv) * 0.35;
    sky += (hash21(gl_FragCoord.xy) - 0.5) * (1.5 / 255.0);
    fragColor = vec4(sky, 1.0);
}
`,B=`
#version 300 es
precision highp float;
uniform vec2  uResolution;
uniform vec3  uSkyZenith;
uniform vec3  uSkyHorizon;
uniform vec2  uSunPos;
uniform vec3  uSunColor;
uniform float uSunElev;
in vec2 vUv;
out vec4 fragColor;
void main() {
    vec2 uv = gl_FragCoord.xy / uResolution;
    vec3 sky = mix(uSkyHorizon, uSkyZenith, pow(clamp(uv.y, 0.0, 1.0), 0.45));
    float sunUp = smoothstep(-0.15, 0.22, uSunElev);
    float sd = length(uSunPos - gl_FragCoord.xy);
    sky += uSunColor * exp(-sd * sd * 0.0002) * 0.35 * sunUp;
    fragColor = vec4(sky, 1.0);
}
`,y=39.9042*Math.PI/180,I=116.4074,k=8;function q(c){const t=c.getTime()+c.getTimezoneOffset()*6e4,s=new Date(t+k*3600*1e3),e=s.getUTCFullYear(),o=Date.UTC(e,0,0),i=Math.floor((Date.UTC(e,s.getUTCMonth(),s.getUTCDate())-o)/864e5),a=s.getUTCHours()+s.getUTCMinutes()/60+s.getUTCSeconds()/3600,n=2*Math.PI/365*(i-1+a/24),u=229.18*(75e-6+.001868*Math.cos(n)-.032077*Math.sin(n)-.014615*Math.cos(2*n)-.040849*Math.sin(2*n)),h=.006918-.399912*Math.cos(n)+.070257*Math.sin(n)-.006758*Math.cos(2*n)+907e-6*Math.sin(2*n)-.002697*Math.cos(3*n)+.00148*Math.sin(3*n),f=(a*15+(I-k*15)+u/4-180)*Math.PI/180,d=Math.sin(y)*Math.sin(h)+Math.cos(y)*Math.cos(h)*Math.cos(f),m=Math.asin(Math.max(-1,Math.min(1,d))),p=(Math.sin(h)-Math.sin(m)*Math.sin(y))/(Math.cos(m)*Math.cos(y)||1e-9);let v=Math.acos(Math.max(-1,Math.min(1,p)));return Math.sin(f)>0&&(v=2*Math.PI-v),{elevation:m,azimuth:v,hours:a}}const g=[{h:0,zen:[.006,.013,.038],hor:[.018,.032,.08],sun:[.5,.55,.95],amb:[.045,.06,.13],gnd:[.015,.026,.055]},{h:4,zen:[.006,.013,.038],hor:[.018,.032,.08],sun:[.5,.55,.95],amb:[.045,.06,.13],gnd:[.015,.026,.055]},{h:5,zen:[.02,.04,.1],hor:[.1,.09,.24],sun:[.8,.7,1.2],amb:[.09,.09,.18],gnd:[.04,.05,.1]},{h:6,zen:[.1,.17,.36],hor:[.9,.6,.45],sun:[1.5,.8,.45],amb:[.2,.17,.24],gnd:[.09,.09,.15]},{h:7,zen:[.16,.25,.47],hor:[.7,.62,.55],sun:[1.25,1,.85],amb:[.28,.29,.38],gnd:[.11,.12,.18]},{h:12,zen:[.12,.22,.45],hor:[.55,.67,.86],sun:[1.05,1,.95],amb:[.38,.44,.56],gnd:[.14,.16,.24]},{h:16,zen:[.15,.25,.46],hor:[.6,.68,.85],sun:[1.02,.99,.95],amb:[.34,.4,.52],gnd:[.13,.15,.22]},{h:18,zen:[.2,.28,.44],hor:[1,.5,.3],sun:[1.6,.55,.25],amb:[.25,.16,.18],gnd:[.1,.08,.12]},{h:19,zen:[.06,.1,.22],hor:[.5,.25,.35],sun:[1.2,.5,.6],amb:[.12,.09,.16],gnd:[.05,.04,.08]},{h:21,zen:[.012,.025,.07],hor:[.05,.07,.17],sun:[.7,.7,1.1],amb:[.06,.07,.14],gnd:[.02,.03,.07]},{h:24,zen:[.006,.013,.038],hor:[.018,.032,.08],sun:[.5,.55,.95],amb:[.045,.06,.13],gnd:[.015,.026,.055]}];function w(c,t,s,e,o){const i=o*o,a=i*o;return .5*(2*t+(-c+s)*o+(2*c-5*t+4*s-e)*i+(-c+3*t-3*s+e)*a)}function G(c){const t=g.length;let s=t-1;for(let r=0;r<t;r++)if(c<g[(r+1)%t].h){s=r;break}const e=g[s%t],o=g[(s+1)%t],i=g[(s-1+t)%t],a=g[(s+2)%t];let n=o.h-e.h;n<=0&&(n+=24);const u=(c-e.h)/n,h=r=>{const f=i[r],d=e[r],m=o[r],p=a[r];return[w(f[0],d[0],m[0],p[0],u),w(f[1],d[1],m[1],p[1],u),w(f[2],d[2],m[2],p[2],u)]};return{zenith:h("zen"),horizon:h("hor"),sun:h("sun"),ambient:h("amb"),ground:h("gnd")}}const V={states:{CLEAR:0,RAIN:1},state:0,target:0,smooth:0,timer:0,init(){this.smooth=0,this.target=0,this.state=this.states.CLEAR,this.timer=18+12*Math.random()},update(c){if(this.timer-=c,this.timer<=0){this.timer=45+45*Math.random();const s=Math.random()<.4;this.target=s?this.states.RAIN:this.states.CLEAR}const t=1/2.5;return this.smooth+=(this.target-this.smooth)*Math.min(1,t*c),this.smooth=Math.max(0,Math.min(1,this.smooth)),this.state=this.smooth>.5?this.states.RAIN:this.states.CLEAR,this.smooth}};class O{constructor(){this.gl=null,this.prog=null,this.uniforms={},this.canvas=null,this.dpr=1,this.width=0,this.height=0,this.vao=null,this.motion=1,this.running=!1,this.raf=0,this.last=0,this.weatherSmooth=0,this.cardData=new Float32Array(A*4),this.cardCount=0,this.marchSteps=b,this.cursor={x:-99999,y:-99999,tx:-99999,ty:-99999,l:0,tl:0},this.scrollY=0,this.state="idle",this.software=!1,this.shaderTier=null,this.ftEma=.016,this.ftAcc=0,this.wfsm=Object.create(V),this.theme=0}setState(t){this.state=t}createContext(){const t={alpha:!1,antialias:!0,depth:!1,stencil:!1,premultipliedAlpha:!1,preserveDrawingBuffer:!1,powerPreference:"high-performance",desynchronized:!0,failIfMajorPerformanceCaveat:!0},s=Object.assign({},t,{powerPreference:"default",failIfMajorPerformanceCaveat:!1,desynchronized:!1});let e=null;try{e=this.canvas.getContext("webgl2",t)}catch{e=null}if(e)return this.software=!1,e;try{e=this.canvas.getContext("webgl2",s)}catch{e=null}return e?(this.software=!0,e):null}boot(){this.setState("probing"),console.log("[RTX] boot @",navigator.userAgent.slice(0,120)),this.canvas=document.createElement("canvas"),this.canvas.id="rtx-canvas",this.canvas.setAttribute("aria-hidden","true"),this.canvas.style.cssText="position:fixed;inset:0;z-index:-1;display:block;width:100%;height:100%;pointer-events:none;background:transparent;";const t=this.createContext();if(!t){this.fail("webgl2-unavailable","\u60A8\u7684\u8BBE\u5907/\u6D4F\u89C8\u5668\u65E0\u6CD5\u521B\u5EFA WebGL 2.0 \u4E0A\u4E0B\u6587\uFF0C\u65E0\u6CD5\u8FD0\u884C\u7269\u7406\u5149\u8FFD\u5F15\u64CE\u3002\u8BF7\u4F7F\u7528\u652F\u6301 WebGL 2.0 \u7684\u73B0\u4EE3\u6D4F\u89C8\u5668\uFF08Chrome / Edge / Safari 15+\uFF09\u540E\u91CD\u8BD5\u3002");return}this.gl=t,document.body.prepend(this.canvas),this.software&&(this.marchSteps=Math.min(this.marchSteps,x.softwareMarch||20),console.warn("[RTX] \u8F6F\u4EF6\u6E32\u67D3 WebGL2\uFF08GPU \u9A71\u52A8\u53EF\u80FD\u88AB\u6D4F\u89C8\u5668\u964D\u7EA7\uFF09\u3002\u5DF2\u81EA\u52A8\u964D\u4F4E\u4F53\u79EF\u5149\u6B65\u6570\u81F3 "+this.marchSteps+"\uFF0C\u771F\u673A\u5EFA\u8BAE\u66F4\u6362\u786C\u4EF6\u52A0\u901F\u6D4F\u89C8\u5668\u83B7\u5F97\u6EE1\u8840\u5149\u8FFD\u3002")),this.compile().then(()=>{this.setupGeometry(),this.setupUniforms(),this.bindEvents(),this.resize(),this.syncCards(),document.documentElement.classList.add("rtx-active"),this.setState("ready"),this.start()}).catch(s=>{const e=String(s&&s.message||s).slice(0,500);console.warn("[RTX] \u5168\u90E8\u7740\u8272\u5668\u6863\u4F4D\u7F16\u8BD1\u5931\u8D25\uFF0C\u56DE\u9000\u9759\u6001\u80CC\u666F\uFF1A",s),this.canvas.remove(),this.fail("shader-fail","\u56FE\u5F62\u5F15\u64CE\u7740\u8272\u5668\u7F16\u8BD1\u5931\u8D25\uFF0C\u5DF2\u56DE\u9000\u9759\u6001\u80CC\u666F\u3002\u5982\u9700\u53CD\u9988\u8BF7\u622A\u56FE\uFF1A\u3010"+e.replace(/\s+/g," ").slice(0,160)+"\u3011",e)})}fail(t,s,e){this.setState("dead"),this.failReason=t,window.__rtxFail={stage:t,message:s,detail:e||"",ua:navigator.userAgent},document.documentElement.classList.add("rtx-fallback");const o=document.createElement("div");o.id="rtx-notice",o.setAttribute("role","status"),o.style.cssText="position:fixed;left:16px;bottom:16px;z-index:9999;max-width:min(420px,calc(100vw-32px));padding:12px 16px;border-radius:14px;font-size:13px;line-height:1.6;color:#e8edff;background:rgba(16,26,48,0.92);border:1px solid rgba(255,255,255,0.16);box-shadow:0 12px 34px rgba(0,0,0,0.35);backdrop-filter:blur(14px);";const i=document.createElement("span");i.textContent=s;const a=document.createElement("button");a.type="button",a.setAttribute("aria-label","\u5173\u95ED\u63D0\u793A"),a.textContent="\xD7",a.style.cssText="margin-left:10px;border:0;background:transparent;color:#9fb0d8;font-size:18px;cursor:pointer;line-height:1;",a.addEventListener("click",()=>o.remove()),o.append(i,a),document.body.appendChild(o)}async compile(){let t=_;E&&(t=await fetch(E.frag).then(o=>o.text()));const s=[["full",t],["lite",H],["mini",B]];let e=null;for(const[o,i]of s)try{this.linkProgram(N,i),this.shaderTier=o,console.log("[RTX] \u7740\u8272\u5668\u6863\u4F4D\u5C31\u7EEA:",o);return}catch(a){e=a,console.warn("[RTX] \u7740\u8272\u5668\u6863\u4F4D "+o+" \u7F16\u8BD1\u5931\u8D25:",a&&a.message)}throw e||new Error("\u6240\u6709\u7740\u8272\u5668\u6863\u4F4D\u7F16\u8BD1\u5931\u8D25")}linkProgram(t,s){const e=this.gl,o=(u,h)=>{const r=e.createShader(u);if(e.shaderSource(r,String(h).trim()),e.compileShader(r),!e.getShaderParameter(r,e.COMPILE_STATUS)){const f=e.getShaderInfoLog(r);throw e.deleteShader(r),new Error(String(f).slice(0,300))}return r};let i=null,a=null,n=null;try{if(i=o(e.VERTEX_SHADER,t),a=o(e.FRAGMENT_SHADER,s),n=e.createProgram(),e.attachShader(n,i),e.attachShader(n,a),e.linkProgram(n),!e.getProgramParameter(n,e.LINK_STATUS))throw new Error(String(e.getProgramInfoLog(n)).slice(0,300));this.prog=n}catch(u){throw n&&e.deleteProgram(n),u}finally{i&&e.deleteShader(i),a&&e.deleteShader(a)}}setupGeometry(){const t=this.gl;this.vao=t.createVertexArray(),t.bindVertexArray(this.vao);const s=t.createBuffer();t.bindBuffer(t.ARRAY_BUFFER,s),t.bufferData(t.ARRAY_BUFFER,new Float32Array([-1,-1,3,-1,-1,3]),t.STATIC_DRAW),t.enableVertexAttribArray(0),t.vertexAttribPointer(0,2,t.FLOAT,!1,0,0),t.bindVertexArray(null)}setupUniforms(){const t=["uResolution","uSunPos","uSunDir","uSunColor","uSkyZenith","uSkyHorizon","uAmbient","uGround","uSunElev","uTime","uScrollY","uDPR","uWeather","uRain","uTheme","uSteps","uCardCount","uCards","uCursor","uCursorL"],s={};for(const e of t)s[e]=this.gl.getUniformLocation(this.prog,e);this.uniforms=s}bindEvents(){window.addEventListener("resize",()=>this.resize(),{passive:!0}),window.addEventListener("orientationchange",()=>this.resize());let t=!1;const s=()=>{t||(t=!0,requestAnimationFrame(()=>{t=!1,this.scrollY=(window.scrollY||0)*this.dpr,this.syncCards()}))};window.addEventListener("scroll",s,{passive:!0});const e=o=>{this.cursor.tx=o.clientX*this.dpr,this.cursor.ty=o.clientY*this.dpr,this.cursor.tl=1};window.addEventListener("pointermove",e,{passive:!0}),window.addEventListener("pointerdown",e,{passive:!0}),document.addEventListener("pointerleave",()=>{this.cursor.tl=0}),window.addEventListener("load",()=>this.syncCards()),"ResizeObserver"in window&&new ResizeObserver(()=>this.syncCards()).observe(document.getElementById("main-content")||document.body),document.addEventListener("visibilitychange",()=>{document.hidden?this.pause():this.resume()}),this.motion=window.matchMedia("(prefers-reduced-motion: reduce)").matches?.2:1}resize(){const t=Math.max(1,window.devicePixelRatio||1);this.dpr=t;const s=Math.max(1,Math.floor(window.innerWidth*t)),e=Math.max(1,Math.floor(window.innerHeight*t));this.width=s,this.height=e,this.canvas&&(this.canvas.width=s,this.canvas.height=e),this.syncCards()}syncCards(){const t=document.querySelectorAll(F),s=this.cardData;let e=0;for(let o=0;o<t.length&&o<A;o++){const i=t[o].getBoundingClientRect(),a=o*4;s[a]=i.left*this.dpr,s[a+1]=i.top*this.dpr,s[a+2]=i.width*this.dpr,s[a+3]=i.height*this.dpr,e++}this.cardCount=e}start(){this.running||(this.running=!0,this.last=performance.now(),this.raf=requestAnimationFrame(t=>this.frame(t)))}pause(){this.running=!1,cancelAnimationFrame(this.raf)}resume(){this.state!=="ready"||this.running||(this.last=performance.now(),this.start())}frame(t){if(!this.running)return;const s=Math.min(.05,(t-this.last)/1e3||.016);this.last=t,this.update(s,t/1e3),this.draw(),this.ftEma+=(s-this.ftEma)*.05,this.ftAcc+=s,this.ftAcc>2&&(this.ftAcc=0,this.tuneMarch()),this.raf=requestAnimationFrame(e=>this.frame(e))}tuneMarch(){if(x.adaptiveMarch===!1)return;const t=this.ftEma*1e3,s=this.marchSteps;t>24&&this.marchSteps>16?this.marchSteps-=16:t<14&&!this.software&&this.marchSteps<b&&(this.marchSteps=Math.min(b,this.marchSteps+8)),this.marchSteps!==s&&console.log("[RTX] \u81EA\u9002\u5E94\u4F53\u79EF\u5149\u6B65\u6570:",s,"->",this.marchSteps,"\u5E27\u65F6\u2248"+t.toFixed(1)+"ms")}update(t,s){const e=this.gl,o=this.uniforms,a=q(new Date),n=G(a.hours),u=document.documentElement.dataset.resolvedTheme==="light"?1:0;this.theme=u;const h=1+u*.7,r=a.azimuth-Math.PI,f=a.elevation,d=Math.sin(r),m=Math.sin(f),p=Math.cos(r),v=Math.hypot(d,m,p),C={x:d/v,y:m/v,z:p/v},z=this.height*1.1,P=Math.max(p,.05),Y=this.width*.5+d/P*z,W=this.height*.5-m/P*z,L=this.wfsm.update(t),K=Math.pow(L,1.5),l=this.cursor;l.l+=(l.tl-l.l)*Math.min(1,t*6);const D=l.x+(l.tx-l.x)*Math.min(1,t*24),U=l.y+(l.ty-l.y)*Math.min(1,t*24);l.x=D,l.y=U;const S=(X,M,R)=>e.uniform3f(o[X],M[0]*R,M[1]*R,M[2]*R);S("uSkyZenith",n.zenith,h),S("uSkyHorizon",n.horizon,h),S("uSunColor",n.sun,1),S("uAmbient",n.ambient,h),S("uGround",n.ground,h*.9),e.uniform2f(o.uResolution,this.width,this.height),e.uniform2f(o.uSunPos,Y,W),e.uniform3f(o.uSunDir,C.x,C.y,C.z),e.uniform1f(o.uSunElev,f),e.uniform1f(o.uTime,s*this.motion),e.uniform1f(o.uScrollY,this.scrollY),e.uniform1f(o.uDPR,this.dpr),e.uniform1f(o.uWeather,L),e.uniform1f(o.uRain,K),e.uniform1f(o.uTheme,u),e.uniform1i(o.uSteps,Math.max(8,Math.min(128,this.marchSteps|0))),e.uniform1i(o.uCardCount,this.cardCount),e.uniform4fv(o.uCards,this.cardData),e.uniform2f(o.uCursor,D,U),e.uniform1f(o.uCursorL,l.l)}draw(){const t=this.gl;t.viewport(0,0,this.width,this.height),t.disable(t.DEPTH_TEST),t.disable(t.BLEND),t.useProgram(this.prog),t.bindVertexArray(this.vao),t.drawArrays(t.TRIANGLES,0,3),t.bindVertexArray(null)}setMarch(t){this.marchSteps=Math.max(8,Math.min(128,t|0))}setWeather(t){this.wfsm.target=t==="rain"?this.wfsm.states.RAIN:this.wfsm.states.CLEAR}}function T(){const c=new O;window.__rtx=c,c.wfsm.init(),c.boot()}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",T):T()})();
