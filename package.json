{
  "name": "yaoxi-personal-page",
  "version": "1.0.0",
  "description": "My Personal Homepage",
  "scripts": {
    "build": "mkdir -p dist && cp -r ./* dist/ --exclude node_modules --exclude .git",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}